server {
    {% for item in server_settings %}
    {{ item['key'] }} {{ item['value'] }};
    {% endfor %}    

    root /var/www/{{ domain_name }};
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }
}


